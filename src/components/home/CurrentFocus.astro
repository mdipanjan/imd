---
import PageRail from '../journal/PageRail.astro';

type FocusItem = {
  label: string;
  value: string;
};

type Props = {
  items: FocusItem[];
};

const { items } = Astro.props;
---

<PageRail title="Right now" {items} />
